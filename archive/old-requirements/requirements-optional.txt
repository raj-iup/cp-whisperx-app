# Optional dependencies for enhanced features
# These are not required but provide additional functionality

# Phonetic matching for bias correction (highly recommended)
# Used by bias_injection stage for sound-alike name correction
jellyfish>=1.0.0

# ML-based glossary selection (recommended for best accuracy)
# Provides semantic embeddings for intelligent term selection
# Falls back to TF-IDF if not installed
sentence-transformers>=2.2.0

# IndicTransToolkit for enhanced Hindi→English translation (recommended)
# Provides better preprocessing and postprocessing for IndicTrans2
# Falls back to basic tokenization if not installed
IndicTransToolkit>=1.0.0

# Note: transformers is already in main requirements.txt
# sentence-transformers provides optimized inference and better accuracy
musicbrainzngs>=0.7.1
