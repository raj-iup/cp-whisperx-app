# Core dependencies for CP-WhisperX-App
# Installation order matters for version compatibility

# 1. NumPy (whisperx 3.4.3+ requires >=2.0.2, works with torchaudio 2.8 at runtime)
numpy>=2.0.2,<2.1

# 2. PyTorch ecosystem (2.8.x works with numpy 2.x despite pip metadata)
# Allows platform-specific optimizations (CUDA/MPS)
torch>=2.8.0,<2.9
torchaudio>=2.8.0,<2.9

# 3. Whisper ecosystem
openai-whisper>=20231117
faster-whisper>=1.0.0
whisper-ctranslate2>=0.4.0
ctranslate2>=4.2.0

# 4. PyAnnote ecosystem (install BEFORE whisperx to control version)
pyannote.audio>=3.3.2,<4.0.0

# 5. WhisperX (compatible with pyannote.audio>=3.3.2)
whisperx>=3.7.0

# 6. HuggingFace ecosystem
huggingface_hub>=0.23.0
huggingface_hub[hf_xet]>=0.23.0

# 7. Audio processing
librosa>=0.10.1
soundfile>=0.12.1

# 8. Metadata and subtitles
tmdbsimple>=2.9.1
tmdbv3api>=1.9.0
pysubs2>=1.1.0

# 9. NER and NLP
spacy>=3.7.0
rapidfuzz>=3.10.0
transformers>=4.30.0

# 10. UI and utilities
rich>=13.7.0
python-dotenv>=1.0.0

# System utilities
psutil>=5.9.0

# Additional utilities
pyyaml>=6.0
requests>=2.31.0
python-json-logger>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
