# macOS Apple Silicon (M1/M2/M3) specific requirements
# For use with ./scripts/bootstrap.sh

# 1. NumPy (must be <2.0 for torchaudio compatibility)
numpy>=1.24.0,<2.0

# 2. PyTorch with MPS support (Apple Silicon optimized)
# Use latest stable with MPS support
torch>=2.3.0,<3.0
torchaudio>=2.3.0,<3.0

# Note: Bootstrap script will auto-downgrade to 2.8.x if needed for PyAnnote compatibility

# 3. Whisper ecosystem
openai-whisper>=20231117
faster-whisper>=1.0.0
whisper-ctranslate2>=0.4.0
ctranslate2>=4.2.0

# 4. PyAnnote ecosystem
pyannote.audio>=3.3.2,<4.0.0

# 5. WhisperX
whisperx>=3.7.0

# 6. HuggingFace ecosystem
huggingface_hub>=0.23.0
huggingface_hub[hf_xet]>=0.23.0

# 7. Audio processing
librosa>=0.10.1
soundfile>=0.12.1

# 8. Metadata and subtitles
tmdbsimple>=2.9.1
tmdbv3api>=1.9.0
pysubs2>=1.1.0

# 9. NER and NLP
spacy>=3.7.0
transformers>=4.30.0
rapidfuzz>=3.10.0

# 10. UI and utilities
rich>=13.7.0
python-dotenv>=1.0.0

# System utilities
psutil>=5.9.0

# Additional utilities
pyyaml>=6.0
requests>=2.31.0
python-json-logger>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
